# Development / CI dependencies. The runtime image installs requirements.txt only.
-r requirements.txt

# Test framework and the HTTP client Starlette's TestClient needs (previously
# provided transitively by fastapi[standard]). Pinned the same way as
# requirements.txt — see the regeneration note there.
pytest==9.1.1
httpx==0.28.1

# Architectural import contracts (see .importlinter). Run with `lint-imports`.
# Also enforced in CI by .github/workflows/lint.yml.
import-linter==2.14

# --- Transitive dependencies of the above (pinned for reproducibility) ---
httpcore==1.0.9
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
grimp==3.16
markdown-it-py==4.2.0
mdurl==0.1.2
rich==15.0.0
