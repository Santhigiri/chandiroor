# Git
.git
.gitignore

# CI
.github

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Environment / secrets
.env
.env.*
!.env.example

# Local dev / editor
.vscode/
.idea/
*.swp
.DS_Store

# Tests & docs
tests/
docs/

# Offline/build-only artifacts not needed at runtime (the app reads from Postgres,
# not these). Keep de421.bsp — it IS loaded at runtime.
data/
db/sql/
scripts/

# Caches not needed in the runtime image
*.log
